---
const currentYear = new Date().getFullYear();
---

<footer class="bg-text text-background/80 py-16 px-6">
    <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-3 gap-12">
        <!-- Brand & Address -->
        <div class="space-y-4">
            <h3 class="font-serif text-2xl text-background">Lumina Stay</h3>
            <address class="not-italic text-sm leading-relaxed opacity-80">
                123 Riverview Terrace,<br />
                Concept District<br />
                (Fictional address)
            </address>
        </div>

        <!-- Contact & Hours -->
        <div class="space-y-4">
            <h4
                class="uppercase tracking-widest text-xs font-bold text-accent-bronze"
            >
                Concierge
            </h4>
            <div class="text-sm space-y-2 opacity-80">
                <p>This is a non-indexed concept demo.</p>
                <p>
                    Visit
                    <a
                        href="https://amplifycreativelab.com"
                        class="underline underline-offset-4 hover:text-white transition-colors"
                        rel="nofollow noopener noreferrer"
                        target="_blank"
                        >amplifycreativelab.com</a
                    >
                </p>
                <p class="pt-2">
                    Guests: 24/7 Support<br />
                    Enquiries: 8am - 8pm AWST
                </p>
            </div>
        </div>

        <!-- Links -->
        <div class="space-y-4 md:text-right">
            <h4
                class="uppercase tracking-widest text-xs font-bold text-accent-bronze"
            >
                Explore
            </h4>
            <nav class="flex flex-col gap-2 text-sm opacity-80 md:items-end">
                <a
                    href={`${import.meta.env.BASE_URL}rooms`}
                    class="hover:text-white transition-colors">The Suites</a
                >
                <a
                    href={`${import.meta.env.BASE_URL}amenities`}
                    class="hover:text-white transition-colors">Amenities</a
                >
                <a
                    href={`${import.meta.env.BASE_URL}location`}
                    class="hover:text-white transition-colors">Location</a
                >
                <a
                    href={`${import.meta.env.BASE_URL}privacy`}
                    class="hover:text-white transition-colors">Privacy Policy</a
                >
            </nav>
        </div>
    </div>

    <div
        class="max-w-7xl mx-auto mt-16 pt-8 border-t border-white/10 text-xs flex flex-col md:flex-row justify-between opacity-50"
    >
        <p>&copy; {currentYear} Lumina Stay. A Demo by Amplify Creative Lab.</p>
        <p>Concept demo only (no real service area).</p>
    </div>
</footer>
